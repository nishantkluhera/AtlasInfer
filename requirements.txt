# AtlasInfer dependencies

# Bounded on both sides — see the rationale in pyproject.toml. Short version: a
# paid cloud run that dies on a dependency bump costs money and hours, and the
# numpy 1.x/2.x ABI split silently breaks `import transformers` via scipy/sklearn.

# Core
torch>=2.4,<2.9
numpy>=1.24,<3

# Model loading
transformers>=4.40,<6
accelerate>=0.26,<2
safetensors>=0.4

# Benchmarking (optional - only needed for benchmark.py)
datasets>=2.16,<5
matplotlib>=3.7,<4

# Testing (dev)
pytest>=7.0.0,<10

# --- Optional, Linux/CUDA-only extras (not installed by default) -------------
# External quant baselines for compare_baselines.py — `pip install -e ".[baselines]"`
#   bitsandbytes optimum gptqmodel autoawq
# Downstream zero-shot accuracy for eval_downstream.py — `pip install -e ".[eval]"`
#   lm-eval>=0.4.0
# The harnesses guard these imports and skip cleanly if they're absent.
